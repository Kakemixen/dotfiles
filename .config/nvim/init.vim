
" mapping
map <space> /
imap jj <Esc>

" settings
set number 
set relativenumber
set encoding=utf-8
set tabstop=4
set shiftwidth=4
set ruler

" In many terminal emulators the mouse works just fine, thus enable it.
if has('mouse')
	set mouse=a
endif
